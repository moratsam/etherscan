package scanner

import (
	"context"

	"github.com/moratsam/etherscan/txgraph/graph"
	"github.com/moratsam/etherscan/pipeline"
)

type blockSource struct {
	blockSub graph.BlockSubscriber
}

func (so *blockSource) Error() error {
	
}
func (so *blockSource) Next(context.Context) bool {}
